#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    double x,y,r;
    cin>>x>>y;
    r=x/y;
    cout<<setprecision(3)<<fixed;
    cout<<r<<" km/l"<<'\n';
    return 0;
}